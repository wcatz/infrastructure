# Enabled applications

enabled:
  prometheus: true
  haproxyIngress: true
  cloudflared: false  # Enable when tunnel credentials are configured
  grafana: true
  externalSecrets: true
  tailscaleOperator: false  # Enable for Tailscale Kubernetes operator
