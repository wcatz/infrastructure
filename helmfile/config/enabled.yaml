# Enabled applications
# Set to false to disable deployment of specific apps

enabled:
  prometheus: true
  haproxyIngress: true
  cloudflared: false  # Enable when tunnel credentials are configured
  grafana: true  # Visualization dashboards for monitoring
  externalSecrets: true  # External secrets operator for secret management
  velero: false  # Enable after configuring cloud storage backend (S3/Azure/GCS)
